# EAS Build Ignore
# This file forces EAS to ignore the Git repository and use local files

node_modules/
.expo/
dist/
web-build/
.eas/
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Ignore the parent git repo specifically if it tries to crawl
.git/
../../
